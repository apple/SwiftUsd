# ``/OpenUSD/SwiftKey/IsValid``

 Does this key refer to a valid notification?

`IsValid` will return true if this key refers to a currently active notification. Revoking the key will make it invalid again.
